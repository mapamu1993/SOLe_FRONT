import { AboutDesign } from "../components/AboutDesign";

const AboutPage = () => {
  // Aquí podríamos añadir lógica futura (como cargar miembros del equipo desde API)
  // Por ahora, solo renderizamos el diseño espectacular.
  
  return <AboutDesign />;
};

export default AboutPage;